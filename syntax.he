// standard comment
/*
 * multi line comment
*/

// THIS IS HOW I WANT THE SYNTAX TO LOOK

use <std.io>
use <math.utils>

fn greet() = print("Hello, ")

fn square(x: int) -> int = x * x

fn compare(a: int, b: int) -> int {
    if (a == 0 || b == 0 || (a < b && a != 0)) {
        -> 0
    }
    -> a + b
}

fn idk(f: func<int>, v: int) -> void {
    -> f(v)
}


let x = 3
let y = 5
let z = compare(x, y)

let message: auto = "Done"

println("z = {z}, message = {message}")
